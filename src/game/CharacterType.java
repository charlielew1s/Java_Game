package game;

/**
 * Determines whether an object, for example the character info list, will store haters, supporters or both.
 */
public enum CharacterType {
    SUPPORTERS_ONLY,
    HATERS_ONLY,
    BOTH;
}
